<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lee.pract.xdemoredis2021529.mapper.LeeMybatisMapper">

    <resultMap id="baseResult" type="com.lee.pract.xdemoredis2021529.entity.Lee">
        <id column="id" property="id"></id>
        <result column="name" property="name"></result>
    </resultMap>

    <select id="selectAllList" parameterType="string" resultMap="baseResult">
        SELECT id, name FROM t_lee WHERE name = #{name}
    </select>

</mapper>